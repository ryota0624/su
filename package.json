{
  "name": "tsutillery",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -f logs/client/*; rm -f logs/server/*; rm -f logs/status/*",
    "compile": "tsc -w --outDir js",
    "clean:src": "rm -f src/**/*.js",
    "client": "node js/client/entry client",
    "client:multi": "node js/client/entry client:multi",
    "client:onlyAttack": "node js/client/entry client:onlyAttack",
    "mergelog": "node js/client/entry mergelog",
    "server": "node js/server/single/index",
    "server:cluster": "node js/server/cluster/index",
    "client:timeGrouped": "node js/client/entry client:timeGrouped",
    "test:client": "node js/client/entry test:client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "artillery": "^1.5.0-5",
    "artillery-core": "^1.8.2",
    "express": "^4.14.0",
    "lodash": "^4.13.1",
    "moment": "^2.13.0"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.0"
  }
}
