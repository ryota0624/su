// import { ProcessStatusRepo } from '../repository/processStatus';
// import { GroupedTime } from '../usecases/groupedTime';


// import { ProcessStatusRepoFS } from '../repository/processStatus';

// const processStatusRepo = new ProcessStatusRepoFS;


export default function mesureController(config: Array<string>) {
  // const repoSetups = config.map(path => processStatusRepo.readFromResource(path));
  // Promise.all(repoSetups).then(() => {
  //   const groupedTimeUsecase = new GroupedTime({ statusRepo: processStatusRepo });
  //   groupedTimeUsecase.run();
  // });
}